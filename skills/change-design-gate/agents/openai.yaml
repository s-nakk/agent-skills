interface:
  display_name: "Change Design Gate"
  short_description: "実装前に設計 HTML を作り、明示承認まで実装を止める"
  default_prompt: "Use $change-design-gate before implementing this change: investigate primary sources, fix the Issue, write the design HTML with pipeline diagram, acceptance criteria and test cases, then stop for approval."
